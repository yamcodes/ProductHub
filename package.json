{
  "name": "producthub",
  "notes": [
    "This field is just for humans to read. It does not affect the package.",
    "Please do NOT include dependencies or devDependencies here. These should be defined in each package/app."
  ],
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2",
    "tsx": "^4.5.0"
  },
  "scripts": {
    "dev:web": "tsx watch apps/web"
  },
  "dependencies": {
    "typescript": "^5.3.2"
  }
}
